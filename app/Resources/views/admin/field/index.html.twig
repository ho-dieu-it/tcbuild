{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_post_index' %}

{% block main %}
    <div class="box">
        <div class="box-header">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                <tr>
                    <th>{{ 'label.page.title'|trans }}</th>
                    <th>{{ 'label.page.slug'|trans }}</th>
                    <th>{{ 'label.page.summary'|trans }}</th>
                    <th><i class="fa fa-cogs"> {{ 'label.actions'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for page in pages %}
                    <tr>
                        <td>{{ page.title }}</td>
                        <td>{{ page.slug }}</td>
                        <td>{{ page.summary }}</td>
                        <td>
                            <div class="item-action s">
                                <a href="{{ path('admin_field_show', { menu_id:menu.id, id: page.id }) }}" class="btn btn-sm btn-default">
                                    {{ 'action.show'|trans }}
                                </a>

                                {% if page.isAuthor(app.user) %}
                                    <a href="{{ path('admin_field_edit', { menu_id:menu.id, id: page.id }) }}" class="btn btn-sm btn-primary">
                                        <i class="fa fa-edit"></i> {{ 'action.edit'|trans }}
                                    </a>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th>{{ 'label.page.title'|trans }}</th>
                    <th>{{ 'label.page.slug'|trans }}</th>
                    <th>{{ 'label.page.summary'|trans }}</th>
                    <th><i class="fa fa-cogs"> {{ 'label.actions'|trans }}</th>
                </tr>
                </tfoot>
            </table>
            <a href="{{ path('admin_field_category_index') }}" class="btn btn-link">
                {{ 'action.menu.back_to_list'|trans }}
            </a>
        </div><!-- /.box-body -->
    </div><!-- /.box -->
{% endblock %}

{% block sidebar %}
    <div class="section actions">
        <a href="{{ path('admin_field_new', {id : menu.id}) }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus"></i> {{ 'action.page.create'|trans }}
        </a>
    </div>

 {#   {{ parent() }}

    {{ show_source_code(_self) }}#}
{% endblock %}
