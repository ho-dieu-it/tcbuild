{#
    By default, forms enable client-side validation. This means that you can't
    test the server-side validation errors from the browser. To temporarily
    disable this validation, add the 'novalidate' attribute:

    {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
#}

{{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="{{ button_label|default('action.page.create'|trans) }}"
           class="{{ button_css|default("btn btn-primary") }}" />

    {% if include_back_to_home_link is not defined or include_back_to_home_link == true %}
        <a href="{{ path('admin_customer_index') }}" class="btn btn-link">
            {{ 'action.page.back_to_list'|trans }}
        </a>
    {% endif %}
{{ form_end(form) }}

